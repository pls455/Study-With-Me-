window.SWMGrades={key:'swm-grades',load(){return SWMTools.loadJSON(this.key,[])},add(g){const x=this.load();x.push({...g,id:crypto.randomUUID?.()||String(Date.now())});SWMTools.saveJSON(this.key,x);return x},average(subject){const x=this.load().filter(g=>!subject||g.subject===subject);if(!x.length)return null;return x.reduce((a,g)=>a+(Number(g.mark)/Number(g.max||100)*100),0)/x.length}};
