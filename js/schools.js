(async()=>{const d=await SWM.loadData();if(!d)return;SWM.bindFavorites();const g=document.querySelector('#schools-grid');g.innerHTML=d.schools.map(s=>`<article class="card"><h2>${SWM.esc(s.name)}</h2><p class="muted">${SWM.esc(s.description)}</p><div class="list">${s.resourceIds.map(id=>{const r=d.resources.find(x=>x.id===id);return r?`<div class="list-item"><span>${SWM.resourceIcon(r.type)} ${SWM.esc(r.title)}</span><a class="btn" target="_blank" rel="noopener noreferrer" href="${SWM.esc(r.url)}">فتح</a></div>`:''}).join('')}</div></article>`).join('')})();
