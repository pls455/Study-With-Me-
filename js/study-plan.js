window.SWMStudyPlan={generate({subjects=[],hours=2,days=6,startDate=new Date()}){const out=[];let day=new Date(startDate);let i=0;for(let d=0;d<days;d++){const parts=[];let remaining=hours*60;while(remaining>=25&&subjects.length){const subject=subjects[i%subjects.length];const mins=Math.min(60,remaining);parts.push({subject,minutes:mins});remaining-=mins;i++}out.push({date:day.toISOString().slice(0,10),items:parts});day.setDate(day.getDate()+1)}return out}};
